# Archived prototype

These files are the unchanged Gemini-generated prototype retained for provenance and comparison. They are not part of the supported application and must not be deployed or run as a public service.

The prototype intentionally preserves its original permissive CORS and Flask debug configuration. Use the hardened implementation in `backend/` and `frontend/`, whose security controls and production instructions are documented in the repository root README.
